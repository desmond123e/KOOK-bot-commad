Kook 全能互动机器人 🤖
** 
 
 
 
一款基于 Kook 开放平台开发的多功能互动机器人，集成签到、积分体系、新闻推送、趣味互动、亲密度培养、比赛竞猜等核心功能，适用于 Kook 语音频道 / 文字频道活跃氛围、沉淀用户。
🌟 核心功能
功能模块
详细说明
📅 签到打卡
每日签到获取积分，连续签到额外奖励，支持签到统计（本周 / 本月签到次数）
💰 积分体系
签到、互动、竞猜获胜可赚取积分，积分可用于兑换频道特权（需自行扩展）
📰 新闻推送
支持实时热点、游戏资讯、科技新闻等分类推送，可配置定时推送或手动触发
🎮 趣味互动
猜数字、成语接龙、骰子抽奖、表情包大战等互动玩法，提升频道活跃度
❤️ 亲密度系统
与机器人 / 频道成员互动增加亲密度，亲密度等级解锁专属回复、积分加成等特权
⚽ 比赛竞猜
支持体育赛事、游戏赛事（如 LOL、CS2）竞猜，积分下注，猜对瓜分奖池
📊 数据查询
个人积分、亲密度、签到记录、竞猜历史查询，频道积分排行榜

📋 环境要求
依赖项
版本要求
说明
Python
3.8 及以上
核心运行环境
Kook OpenAPI SDK
1.0.0+
Kook 官方 Python SDK（或第三方封装）
数据库
SQLite3（默认）/ MySQL
存储用户数据、积分、竞猜记录等
网络环境
可访问 Kook 开放平台
需配置防火墙放行 Kook API 域名
可选依赖
Aiohttp、Requests、Schedule
异步请求、定时任务支持

🚀 快速开始
1. 前置准备
登录 Kook 开放平台，创建机器人应用，获取 Bot Token 和 机器人 ID
为机器人添加权限：发送消息、读取消息、管理频道（可选）、上传文件（可选）
记录机器人邀请链接，邀请至目标 Kook 服务器
2. 项目部署
方式一：本地部署
# 1. 克隆项目
git clone https://github.com/desmond123e/KOOK-bot-commad.git
cd kook-all-in-one-bot

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置环境变量（关键）
# 创建 .env 文件，填写以下内容（或直接修改 config.py）
cat > .env <KOOK_BOT_TOKEN=你的机器人Token
KOOK_BOT_ID=你的机器人ID
DB_TYPE=sqlite3  # 可选：mysql
DB_HOST=localhost  # MySQL 时需填写
DB_USER=root       # MySQL 时需填写
DB_PASS=密码       # MySQL 时需填写
DB_NAME=kook_bot   # 数据库名称
NEWS_API_KEY=你的新闻API密钥（如聚合数据、百度新闻API）
EOF

# 4. 初始化数据库
python scripts/init_db.py

# 5. 启动机器人
python main.py

方式二：Docker 部署（推荐）
# 1. 构建镜像
docker build -t kook-bot:latest .

# 2. 启动容器（自动初始化数据库）
docker run -d \
  --name kook-bot \
  -e KOOK_BOT_TOKEN=你的机器人Token \
  -e KOOK_BOT_ID=你的机器人ID \
  -e NEWS_API_KEY=你的新闻API密钥 \
  -v ./data:/app/data  # 持久化数据库
  kook-bot:latest

3. 验证运行
机器人启动后，在 Kook 频道发送 !help 或 @机器人 help，收到回复即部署成功
首次启动自动创建默认管理员账号（可在 config.py 配置管理员 Kook ID）
📂 目录结构
├── main.py                # 机器人入口文件（初始化客户端、注册指令）
├── config.py              # 全局配置（环境变量读取、默认参数）
├── requirements.txt       # 依赖清单
├── .env.example           # 环境变量示例
├── core/                  # 核心模块
│   ├── bot.py             # Kook 客户端初始化、事件监听
│   ├── database/          # 数据库操作（用户、积分、竞猜等模型）
│   ├── commands/          # 指令处理（按功能模块拆分）
│   │   ├── sign.py        # 签到相关指令
│   │   ├── points.py      # 积分相关指令
│   │   ├── news.py        # 新闻相关指令
│   │   ├── interaction.py # 趣味互动指令
│   │   ├── intimacy.py    # 亲密度相关指令
│   │   └── bet.py         # 比赛竞猜指令
│   ├── tasks/             # 定时任务（新闻推送、签到重置等）
│   └── utils/             # 工具函数（API请求、格式化、权限校验）
├── scripts/               # 辅助脚本（数据库初始化、数据导入）
├── data/                  # 数据存储（SQLite数据库文件、日志）
└── docs/                  # 详细文档（扩展开发、API对接说明）
